---
"@zooid/web": minor
---

Read the initial workforce space from `workforce_space` in runtime `config.json` (default `dev`). The build-time `VITE_WORKFORCE_SPACE` setting is removed, so one bundle can serve several vhosts.
