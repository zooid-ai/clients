---
"@zooid/web": minor
---

Share dialog: pick the room from a search field with autocomplete, and write the comment in the composer's input so `@mentions` of the target room's members work. The composer's input core is now a shared `MessageInput`.
